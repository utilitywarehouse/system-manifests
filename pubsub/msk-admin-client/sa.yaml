# Used by tf-kafka-config-admin to fetch resources in this namespace
apiVersion: v1
kind: ServiceAccount
metadata:
  name: msk-admin-client
# add the vault role annotation to gain access to the tf state bucket in the specific environment
#  annotations:
#    vault.uw.systems/aws-role: "arn:aws:iam::950135041896:role/pubsub-tf-applier-state-bucket-rw"
