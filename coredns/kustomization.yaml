apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

bases:
  - upstream/

patchesStrategicMerge:
  - patches/ignore.yaml

resources:
  - resources/pdb.yaml
  - resources/psp.yaml
  - resources/rbac.yaml
  - resources/svc-metrics.yaml
