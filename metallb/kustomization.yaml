apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
#namespace: sys-metallb
patchesStrategicMerge:
  - patches/delete-cluster-rbac.yaml
  - patches/delete-psp.yaml
  - patches/delete-role-rbac.yaml
  - patches/deploy-controller.yaml
  - patches/ds-speaker.yaml
resources:
  - ds-master-speaker.yaml
  - metallb.yaml
