apiVersion: batch/v1
kind: CronJob
metadata:
  name: in-another-namespace
  namespace: not-dev-enablement
spec:
  schedule: "* * * * *"
  jobTemplate:
    spec:
      template:
        metadata:
        spec:
          containers:
            - name: job-container
              image: my-job-image:latest
