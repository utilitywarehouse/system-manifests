apiVersion: batch/v1
kind: CronJob
metadata:
  name: in-another-namespace
  namespace: kube-system
spec:
  schedule: "* * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: job-container
              image: my-job-image:latest
