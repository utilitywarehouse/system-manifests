apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: in-another-namespace
  namespace: not-dev-enablement
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: elasticsearch
          image: elasticsearch:latest
