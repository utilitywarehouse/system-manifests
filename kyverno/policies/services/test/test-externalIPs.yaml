apiVersion: v1
kind: Service
metadata:
  name: test-externalip-not-allowed
spec:
  type: LoadBalancer
  externalIPs:
    - 1.1.1.1
  ports:
    - name: dummy
      port: 30000
      protocol: TCP
      targetPort: 30000
